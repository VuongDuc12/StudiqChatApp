export default function StudyLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="study-area">
      {/* Header/menu cho study user */}
      {children}
    </div>
  );
} 